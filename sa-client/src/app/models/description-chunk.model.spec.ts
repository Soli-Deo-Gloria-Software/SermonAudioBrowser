import { DescriptionChunk } from './description-chunkmodel';

describe('DescriptionChunk', () => {
  it('should create an instance', () => {
    expect(new DescriptionChunk()).toBeTruthy();
  });
});
