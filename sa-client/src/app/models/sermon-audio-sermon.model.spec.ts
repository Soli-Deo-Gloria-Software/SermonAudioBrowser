import { SermonAudioSermon } from './sermon-audio-sermon.model';

describe('SermonAudioSermon', () => {
  it('should create an instance', () => {
    expect(new SermonAudioSermon()).toBeTruthy();
  });
});
