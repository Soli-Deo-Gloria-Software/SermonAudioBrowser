import { EsvPassageMetadata } from './esv-passage-metadata.model';

describe('EsvPassageMetadata', () => {
  it('should create an instance', () => {
    expect(new EsvPassageMetadata()).toBeTruthy();
  });
});
