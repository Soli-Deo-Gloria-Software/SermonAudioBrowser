import { SermonAudioSeries } from './sermon-audio-series.model';

describe('SermonAudioSeries', () => {
  it('should create an instance', () => {
    expect(new SermonAudioSeries()).toBeTruthy();
  });
});
