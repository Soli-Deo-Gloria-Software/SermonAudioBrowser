import { SermonAudioBroadcaster } from './sermon-audio-broadcaster.model';

describe('SermonAudioBroadcaster', () => {
  it('should create an instance', () => {
    expect(new SermonAudioBroadcaster()).toBeTruthy();
  });
});
