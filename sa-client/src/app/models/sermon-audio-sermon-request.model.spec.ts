import { SermonAudioSermonRequest } from './sermon-audio-sermon-request.model';

describe('SermonAudioSermon', () => {
  it('should create an instance', () => {
    expect(new SermonAudioSermonRequest()).toBeTruthy();
  });
});
