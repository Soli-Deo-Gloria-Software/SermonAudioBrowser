import { SermonAudioSpeaker } from './sermon-audio-speaker.model';

describe('SermonAudioSpeaker', () => {
  it('should create an instance', () => {
    expect(new SermonAudioSpeaker()).toBeTruthy();
  });
});
