import { SermonAudioMediaCollection } from './sermon-audio-media-collection.model';

describe('SermonAudioMediaCollection', () => {
  it('should create an instance', () => {
    expect(new SermonAudioMediaCollection()).toBeTruthy();
  });
});
